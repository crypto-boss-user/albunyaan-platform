# /platform-campaign — the Uscreen-exit goal

Load and execute the campaign skill: read `.claude/skills/albunyaan-platform-replacement-campaign/SKILL.md` in full before doing anything else.

Then:
1. Run the Phase 0 status snapshot commands to establish the CURRENT state (do not trust the skill's date-stamped numbers — re-measure).
2. Report which phase the campaign is in, which gates are passed (with the measured numbers as evidence), and what the single next action is.
3. Before executing any next action, check the phase's MODEL FITNESS gate and `albunyaan-change-control` tier: if the action is escalation- or founder-tier, STOP and say so instead of proceeding.
4. If any observation contradicts the skill (drifted number, missing file, changed behavior), say so explicitly and propose the skill edit — do not silently work around it.

The goal, verbatim from the founder (2026-07-12): a COMPLETE, fully functioning replacement of Albunyaan's entire Uscreen platform — improved, not cloned — databases, apps, website, front end, back end, everything, so it actually replaces Uscreen and works in every way with nothing forgotten.
