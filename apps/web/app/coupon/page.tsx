import StubPage from '../../components/StubPage';

export const metadata = { title: 'Coupon — Albunyaan TV' };

export default function Page() {
  return (
    <StubPage
      label="Gift"
      title="Coupon"
      blurb="Redeem a coupon or gift a subscription — every subscription is sadaqah invested back into da'wah."
    />
  );
}
