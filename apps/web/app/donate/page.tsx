import StubPage from '../../components/StubPage';

export const metadata = { title: 'Donate — Albunyaan TV' };

export default function Page() {
  return (
    <StubPage
      label="Support"
      title="Donate"
      blurb="Support the platform with a donation — a sadaqah jaariyah that keeps safe Islamic content available for every family."
    />
  );
}
