node_modules/
.next/
dist/
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
.vercel/
worker/out/
worker/raw/
worker/node_modules/
# contains real subscriber PII (names/emails) from Uscreen export — never commit
worker/fixtures/uscreen-people-export.csv
.env*
# bunny keys live in ~/.albunyaan-cc/cloud.env (external, never committed)
.vercel
worker/fixtures/uscreen-leads-export.csv
# contains live IPTV credentials (single shared login) — never commit
infra/live-relay/DRAFT-source-urls-from-ayyadonline.md

# gegenereerde rapporten/bladerversies: tientallen MB's en ze bevatten
# tijdelijk geldige Bunny-speeltokens — nooit committen
var/
